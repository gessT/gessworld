【目標】：
我會提供一張圖片（圖片中可能有多張照片），請你根據圖片生成一個完整的相冊資料 + 部落格文章，要求如下：

【相冊資料要求】：
1. 使用繁體中文。
2. Country / CountryCode / Region / City 必須基於我提供的圖片地點推斷，City = Region。
3. Album Description 需要概述圖片中場景、氛圍、特色。
4. cover_photo_id 先設為 null。
5. 照片陣列命名為 DUMMY_PHOTOS，每張照片：
   - url：示例格式 https://snaptogoclub.s3.ap-southeast-1.amazonaws.com/photos/{region}/{filename}.jpg
   - title：根據照片名稱
   - description：根據照片描述
   - fullAddress：簡單描述拍攝地點
   - visibility: "public"
   - isFavorite: true
   - aspectRatio: 1.5
   - width: 4000
   - height: 2667
   - country / countryCode / region / city 繼承 Album
   - make: "SONY"
   - model: "A6700"
   - lensModel: "Sigma 18-50mm F2.8"
   - dateTimeOriginal: 當前時間

【部落格生成要求】：
1. 使用繁體中文。
2. 格式：Editor `<p1>` 標籤包裹段落。
3. 可以穿插 DUMMY_PHOTOS 的 url，例如：
   <p1>文字描述…<img src="DUMMY_PHOTOS[0].url" alt="DUMMY_PHOTOS[0].title"></p1>
4. 部落格內容必須基於照片資料，不可編造不存在的場景或物件。
5. 段落可以加入 emoji，增加部落格可讀性。
6. 結尾需有總結段落，描述整體氛圍與感受。

【輸出結構】：
1. ALBUM 常量
2. DUMMY_PHOTOS 陣列
3. NEW_POST 常量，包括：
   - title：部落格標題
   - slug：自動生成，例如地名 + 主題
   - author：Your Name
   - publishedAt：當前時間
   - cover_photo_url：DUMMY_PHOTOS[0].url
   - albumId：ALBUM.id 或 null
   - content：完整部落格內容（Editor 格式 <p1>，可穿插照片）不一定要穿插全部照片， 50-60% 的照片穿插在部落格內容中即可。字内容多一点

【注意】：
- DUMMY_PHOTOS[0].url 一致 小写 {region}
- title / description / fullAddress 嚴格基於我提供的照片信息。
- title": 根据照片给一个适合的标题，描述照片内容和氛围。
- description: 根据照片内容描述照片的细节和氛围。
-author ： Gesstue
- 所有相片繼承 Album 的國家/城市資訊。
- 可以生成多張照片，只要將 DUMMY_PHOTOS 物件複製。
- 日期使用當前時間 new Date()。
- 最后生成 2 个json object，分別為 ALBUM 和 NEW_POST，並且 DUMMY_PHOTOS 是 ALBUM 的一部分。
- ALBUM 必须生成 全部照片数据， NEW_POST 的 content 部分可以穿插部分照片（50-60%），但不需要全部照片都穿插在部落格內容中。
- 最重要 做成 json object 我需要， 分开newpost 跟DUMMY_PHOTOS。album
- 如图是英文地点， 可以翻译成中文， 例如： "New York" -> "纽约"， "Paris" -> "巴黎"， "Tokyo" -> "东京" 等等。


【示例輸出】：
```javascript
const ALBUM = {
  country: "",
  countryCode: "",
  region: "",
  city: "",
  description: "",
  cover_photo_id: null
};

const DUMMY_PHOTOS = [
  {
    url: "",
    title: "",
    description: "",
    visibility: "public",
    isFavorite: true,
    aspectRatio: 1.5,
    width: 4000,
    height: 2667,
    country: ALBUM.country,
    countryCode: ALBUM.countryCode,
    region: ALBUM.region,
    city: ALBUM.city,
    fullAddress: "",
    make: "SONY",
    model: "A6700",
    lensModel: "Sigma 18-50mm F2.8",
    dateTimeOriginal: new Date()
  }
];

const NEW_POST = {
  title: "",
  slug: "",
  author: "Your Name",
  publishedAt: new Date(),
  cover_photo_url: DUMMY_PHOTOS[0].url,
  albumId: ALBUM.id || null,
  content: `
<p1>...</p1>
<p1>可穿插 <img src="\${DUMMY_PHOTOS[0].url}" alt="\${DUMMY_PHOTOS[0].title}"></p1>
<p1>...</p1>
<p1>結尾總結段落</p1>
`
};

Content layout should be dynamic.
You can freely design sections such as:
hero image, two-column layout, gallery grid, quote block, story section.

The blog should not always follow the same layout.
Use images from DUMMY_PHOTOS throughout the content.